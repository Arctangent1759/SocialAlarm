<h1>Listing alarms</h1>

<table class='table'>
  <tr>
    <th>Alarm time</th>
    <th>Expected</th>
    <th>Sessions</th>
    <th>Expected</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @alarms.each do |alarm| %>
  <tr>
    <td><%= alarm.alarm_time %></td>
    <td><%= alarm.expected_sessions %></td>
    <td><%= alarm.sessions %></td>
    <td><%= alarm.expected %></td>
    <td><%= link_to 'Show', alarm %></td>
    <td><%= link_to 'Edit', edit_alarm_path(alarm) %></td>
    <td><%= link_to 'Destroy', alarm, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Alarm', new_alarm_path %>
